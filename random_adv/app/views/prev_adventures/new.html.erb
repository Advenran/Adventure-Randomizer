<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Document</title>
</head>
<body>

	<div class="row">

		<div class="col-md-3" id="left-bar">
			<h3>Welcome, <%= @current_user.name %>! </h3>
			<ul>
				<p><%= link_to "Past Adventures", user_prev_adventures_path(@current_user) %></p>
				<p><%= link_to "Edit Profile", edit_user_path(@current_user) %></p>
			</ul>
			<h3><img src="<%=@icon%>">&nbsp <%= @current_forecast %> </h3>
		</div>





		<div class="col-md-6">
			<div class="big">
				<div class="main">
			<h2>Enter your budget, number of people and the timeframe to start</h2>

			<%= form_tag %>
			<div class="form-group">
				Enter your $
				<%= text_field_tag :amount_spend, "", class: "form-control" %>
			</div>
			<div class="form-group">
				Enter amount of people
				<%= text_field_tag :amount_people, "", class: "form-control" %>
			</div>
			<div class="form-group">
				Enter minutes for adventure
				<%= text_field_tag :amount_time, "", class: "form-control" %>
			</div>
			<div class="form-group">
				How would you like to get there?
				<%= select_tag :mode_transportation, options_for_select([ 'walk', 'bike', 'subway', 'drive' ], 'walk'), class: "form-control" %>
			</div>
				<%= submit_tag "submit", class: "btn btn-default"%>
		
</div>
</div>

		</div>
		<div class="col-md-3" id="right-bar">
		</div>
	</div>

</body>
</html>